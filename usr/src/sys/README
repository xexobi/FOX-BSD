amd64/         ← port 64-bit 
conf/          ← build config & file list
kern/          ← kernel core
dev/           ← device driver
ufs/           ← filesystem
vm/            ← memory manager
libkern/       ← libc internal
sys/           ← header & types
net/           ← opsional (abstraksi net)
netinet/       ← TCP/IP v4 (jika pakai jaringan)

